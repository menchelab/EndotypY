#!/bin/zsh
# UV Installation Setup for KL-Clustering Project
# Add this to your ~/.zshrc to make uv commands available globally

# Alias for easy access to uv
alias uv="/Users/berksakalli/.local/bin/uv"

# Optional: Add to PATH
export PATH="/Users/berksakalli/.local/bin:$PATH"
